# ═══════════════════════════════════════════════════════════════
# ANILOGUE Premium SEO & Web Performance Optimization Config
# ═══════════════════════════════════════════════════════════════

# Disable Directory Listings
Options -Indexes

# Rewrite engine configuration
<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Rewrite Sitemap to dynamic PHP generator
    RewriteRule ^sitemap\.xml$ sitemap.php [L]
    
    # 1. Externally redirect explicit /filename.php to /filename (except API endpoints)
    RewriteCond %{REQUEST_URI} !/api/ [NC]
    RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s/+(.*?)\.php[\s?] [NC]
    RewriteRule ^ %1 [R=301,L]

    # 2. Externally redirect direct requests for index to /
    RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s/+(.*?)/?index[\s?] [NC]
    RewriteRule ^ %1/ [R=301,L]

    # 3. Internally rewrite /filename to /filename.php if it exists as a file
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME}\.php -f
    RewriteRule ^(.*?)/?$ $1.php [L]
</IfModule>

# Compress HTML, CSS, JavaScript, Text, XML and Fonts
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE text/javascript
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE image/svg+xml
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/atom+xml
    AddOutputFilterByType DEFLATE application/vnd.ms-fontobject
    AddOutputFilterByType DEFLATE application/x-font-ttf
    AddOutputFilterByType DEFLATE font/opentype
</IfModule>

# Browser Caching Policy (Leverage Browser Caching)
<IfModule mod_expires.c>
    ExpiresActive On
    
    # CSS and JavaScript
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType text/javascript "access plus 1 month"
    ExpiresByType application/x-javascript "access plus 1 month"
    
    # Images and Media
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    
    # Defaults
    ExpiresDefault "access plus 2 days"
</IfModule>
