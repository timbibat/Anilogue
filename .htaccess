# ═══════════════════════════════════════════════════════════════
# ANILOGUE Premium SEO & Web Performance Optimization Config
# ═══════════════════════════════════════════════════════════════

# Disable Directory Listings
Options -Indexes

# Rewrite engine configuration (Explicit, high-compatibility clean URLs)
<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # 1. Rewrite sitemap.xml to dynamic PHP generator
    RewriteRule ^sitemap\.xml$ sitemap.php [L]
    
    # 2. Redirect explicit requests for index.php to the root domain /
    RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s/+(.*?)/?index\.php[\s?] [NC]
    RewriteRule ^ %1/ [R=301,L]
    
    # 3. Redirect explicit requests for details.php to clean /details
    RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s/+(.*?)/?details\.php[\s?] [NC]
    RewriteRule ^ %1/details [R=301,L]
    
    # 4. Internally map /details to details.php seamlessly (QSA preserves query strings)
    RewriteRule ^details/?$ details.php [L,QSA]
</IfModule>

# Compress HTML, CSS, JavaScript, Text, XML and Fonts
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE text/javascript
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE image/svg+xml
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/atom+xml
    AddOutputFilterByType DEFLATE application/vnd.ms-fontobject
    AddOutputFilterByType DEFLATE application/x-font-ttf
    AddOutputFilterByType DEFLATE font/opentype
</IfModule>

# Browser Caching Policy (Leverage Browser Caching)
<IfModule mod_expires.c>
    ExpiresActive On
    
    # CSS and JavaScript
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType text/javascript "access plus 1 month"
    ExpiresByType application/x-javascript "access plus 1 month"
    
    # Images and Media
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    
    # Defaults
    ExpiresDefault "access plus 2 days"
</IfModule>
